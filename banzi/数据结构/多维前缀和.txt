//前缀和是这样（差分就反过来就行，加变减，数组顺序逆序）
    FOR(k, 1, n) {
        FOR(i, 1, n) {
            FOR(j, 1, n) {
                w[k][i][j] += w[k - 1][i][j];
            }
        }
    }

    FOR(k, 1, n) {
        FOR(i, 1, n) {
            FOR(j, 1, n) {
                w[k][i][j] += w[k][i - 1][j];
            }
        }
    }

    FOR(k, 1, n) {
        FOR(i, 1, n) {
            FOR(j, 1, n) {
                w[k][i][j] += w[k][i][j - 1];
            }
        }
    }

