# 内容
- 并查集的p[x]表示的是x的集合代表元，然后路径压缩就是把集合的每个元素都指向它的代表元。
```C++
int find(int x){
    //这句话就是集合的代表元没找到的话，就继续去找。这个find(p[x])就是能找到集合的代表元，也就是这棵树的根
    // 根能找到就直接赋。
    if(x!=p[x])p[x]=find(p[x]);
    //反之就是x==p[x]也就是x是整棵树的根。
    return p[x];
}

```

# 题单
abc451f
LGP10734